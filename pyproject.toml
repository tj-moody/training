[project]
name = "training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "fpdf>=1.7.2",
    "requests>=2.32.3",
    "tabulate>=0.9.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
